namespace Crm.Domain.UserAgg.Enums
{
    /// <summary>
    /// this enum show us user is Teacher or not
    /// if user is teacher leveluser = 0
    /// if user is student leveluser = 1
    /// </summary>
    public enum LevelUser
    {
        Teacher = 0,
        Student = 1
    }
}
