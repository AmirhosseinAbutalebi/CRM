namespace Crm.Domain.TicketDetailAgg.Enums
{
    /// <summary>
    /// define enum statusRead that be 1 for ticket readed and 
    /// 0 for other status (means didn't read the ticket :) )
    /// </summary>
    public enum StatusRead
    {
        Read = 1,
        other = 0
    }
}
